package net.formiguinhas.net;

import org.apache.log4j.Logger;

public class Pagamento extends _Pagamento {
        @SuppressWarnings("unused")
        private static Logger log = Logger.getLogger(Pagamento.class);
}
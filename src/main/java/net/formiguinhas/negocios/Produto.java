package net.formiguinhas.negocios;

import org.apache.log4j.Logger;

public class Produto extends _Produto {
        @SuppressWarnings("unused")
        private static Logger log = Logger.getLogger(Produto.class);
}
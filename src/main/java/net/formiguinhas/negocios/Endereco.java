package net.formiguinhas.negocios;

import org.apache.log4j.Logger;

public class Endereco extends _Endereco {
        @SuppressWarnings("unused")
        private static Logger log = Logger.getLogger(Endereco.class);
}
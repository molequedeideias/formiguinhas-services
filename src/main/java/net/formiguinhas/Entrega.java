package net.formiguinhas;

import org.apache.log4j.Logger;

public class Entrega extends _Entrega {
        @SuppressWarnings("unused")
        private static Logger log = Logger.getLogger(Entrega.class);
}